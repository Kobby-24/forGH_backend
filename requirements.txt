# --- Web Framework & Server ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
gunicorn>=21.0.0

# --- Database (Neon/Postgres) ---
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.0

# --- Audio & Media ---
ffmpeg-python
shazamio

# --- Compatibility Fixes ---
# Only install pyaudioop if Python is 3.13 or newer (required for pydub/shazamio)
pyaudioop; python_version >= '3.13'

# --- Utilities ---
python-dotenv>=1.0.0
requests>=2.31.0
httpx>=0.27.0
apscheduler>=3.10.0
sentry-sdk>=2.0.0
websockets>=12.0

# --- Security & Auth ---
# Note: You have both pyjwt and python-jose listed. 
# Usually you only need one, but I have included commonly used ones here.
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.9
email-validator>=2.1.0
# ... (other dependencies)

# --- Security & Auth ---
# The modern password hasher (add argon2-cffi for best security)
pwdlib[argon2]>=0.3.0

# The classic password hasher (keep if your old code uses CryptContext)
passlib[bcrypt]>=1.7.4

# ...